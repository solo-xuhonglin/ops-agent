# 远程部署 / 运维脚本的 SSH 连接凭据模板。
#
# 使用方式：
#   cp deploy-remote.env.example deploy-remote.env   # 然后填入真实值
#
# deploy-remote.env 已被 .gitignore 忽略，切勿提交。
# 以下三者共用这份配置：
#   - deploy-remote.sh   （自动 source）
#   - deploy-remote.py   （自动读取）
#   - scripts/*.py       （通过 scripts/_conn.py 读取）
# 任何一项也可用同名环境变量覆盖，环境变量优先级更高。

# 必填：服务器 IP 或域名
REMOTE_HOST=
# 必填：SSH 登录密码（若使用密钥登录，deploy-remote.sh 可留空；scripts/*.py 需要密码）
SSHPASS=

# 可选：默认 root
REMOTE_USER=root
# 可选：默认 22
REMOTE_PORT=22
# 可选：服务器上的项目目录，默认 /opt/ops-agent
REMOTE_DIR=/opt/ops-agent
